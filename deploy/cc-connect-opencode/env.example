# Copy to /etc/cc-connect-opencode/env on the VPS and chmod 600.
# Fill values on the VPS only. Do not commit real values.

TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOW_FROM=
TELEGRAM_ALLOW_CHAT=
OPENCODE_MODEL=openai/gpt-5.5
